export class Resto {
   
    public name: string
    public address: string
    public email: string
    constructor( name: string, address: string, email: string) {
    
        this.name = name
        this.address = address
        this.email = email
    }
}